# AGENTS.md

## Project

- 이 프로젝트는 Spring을 사용하는 백엔드 서버로 구성한다.
- Java, Spring Boot, Gradle 기반의 서버 애플리케이션으로 다루며, API, 도메인 로직, 데이터 접근, 외부 연동을 명확히 분리하는 방향으로 개발한다.
- Spring은 애플리케이션 실행과 외부 기술 연동을 돕는 프레임워크이며, 핵심 비즈니스 규칙을 Spring에 종속시키지 않는다.

## Work Scope

- 절대 사용자가 명령하지 않은 내용을 임의로 작업해서는 안 된다.
- 작업 범위는 사용자의 명령에 포함된 파일, 기능, 버그, 문서, 테스트로 제한한다.
- 잘 모르는 내용, 확인하지 못한 내용, 구체적이지 않아서 모호한 부분은 추측해서 단정하지 않는다.
- 모호하거나 위험한 부분은 반드시 모른다고 말하고, 필요한 경우 사용자에게 확인 질문을 한다.
- 사용자의 요청을 수행하기 위해 불가피하게 추가 작업이 필요하다면, 먼저 그 이유와 범위를 사용자에게 설명하고 허가를 받아야 한다.

## Design Principles

- 안정성을 위해 클린 아키텍처(Clean Architecture)와 SOLID 원칙을 함께 지켜야 한다.
- 클린 아키텍처는 관심사를 계층으로 분리하고, 비즈니스 규칙을 프레임워크, UI, 데이터베이스, 외부 시스템 같은 세부 구현으로부터 독립시키는 아키텍처 원칙이다.
- 핵심 규칙은 의존성 규칙(Dependency Rule)이다. 소스 코드 의존성은 바깥 계층에서 안쪽 계층으로만 향해야 하며, 안쪽 계층은 바깥 계층의 구체 타입, 프레임워크, 데이터 형식, 외부 시스템을 알면 안 된다.
- 일반적으로 안쪽에는 엔티티와 유스케이스 같은 비즈니스 정책을 두고, 바깥쪽에는 인터페이스 어댑터, 웹, 데이터베이스, 프레임워크, 외부 연동 같은 세부 구현을 둔다.
- Spring은 애플리케이션의 핵심 정책이 아니라 외부 세부 구현에 속하는 도구로 취급한다. 도메인과 유스케이스가 Spring, JPA, Web, DB 세부 구현에 직접 의존하지 않도록 설계한다.
- 도메인 계층은 엔티티, 값 객체, 도메인 규칙, 도메인 서비스를 포함하며 외부 기술을 몰라야 한다.
- 애플리케이션 계층은 유스케이스를 표현하고, 입력을 받아 도메인 규칙을 실행하며, 필요한 외부 기능은 포트 또는 인터페이스로 의존한다.
- 인터페이스 어댑터 계층은 Controller, DTO, Mapper, Presenter, Repository 구현체처럼 외부 입출력과 내부 유스케이스 사이의 변환을 담당한다.
- 인프라스트럭처 계층은 Spring 설정, JPA, DB, 외부 API, 메시징, 파일 시스템 같은 세부 구현을 담당한다.
- 의존성 역전 원칙을 활용해 안쪽 계층이 인터페이스를 정의하고, 바깥 계층이 그 인터페이스를 구현하게 한다.
- 단일 책임 원칙(SRP): 하나의 클래스, 모듈, 함수는 하나의 변경 이유만 가져야 한다.
- 개방-폐쇄 원칙(OCP): 기존 코드를 불필요하게 수정하지 않고 확장으로 새로운 동작을 추가할 수 있어야 한다.
- 리스코프 치환 원칙(LSP): 하위 타입은 상위 타입을 사용하는 코드의 기대 동작을 깨지 않고 대체될 수 있어야 한다.
- 인터페이스 분리 원칙(ISP): 사용하지 않는 메서드에 의존하도록 큰 인터페이스를 강요하지 않고, 목적에 맞는 작은 인터페이스로 분리한다.
- 의존성 역전 원칙(DIP): 상위 수준 정책은 하위 수준 구현에 직접 의존하지 않고, 둘 다 추상화에 의존해야 한다.

## TDD Core

- TDD(테스트 주도 개발)는 구현하려는 동작을 검증하는 자동화 테스트를 먼저 작성하고, 그 테스트가 실패함을 확인한 뒤, 테스트를 통과시키는 최소한의 코드를 작성하고, 이후 테스트가 계속 통과하는 상태에서 설계를 개선하도록 리팩토링하는 개발 방식이다.
- 핵심 순서는 실패하는 테스트 작성, 최소 구현, 리팩토링이다.
- 실패하는 테스트 작성 단계에서는 사용자의 요구사항을 검증 가능한 기대 동작으로 바꾸고, 아직 구현되지 않았기 때문에 테스트가 실패해야 한다.
- 최소 구현 단계에서는 테스트를 통과시키는 데 필요한 코드만 작성한다. 이 단계에서 불필요한 기능, 과도한 구조, 미래를 위한 추상화는 추가하지 않는다.
- 리팩토링 단계에서는 모든 테스트가 통과하는 상태를 유지하면서 중복 제거, 명명 개선, 책임 분리, 의존성 방향 정리, 가독성 개선을 수행한다.
- TDD는 테스트 자체가 목적이 아니라, 요구사항을 작은 단위로 확인하고 안정적으로 설계를 개선하기 위한 개발 흐름이다.
- 테스트 작성, 최소 구현, 리팩토링의 순환을 필요한 동작이 완성될 때까지 반복한다.

## Simplicity

- 추상화, 클린 아키텍처, SOLID, 테스트 가능성 같은 개발 원칙을 전반적으로 지켜야 한다.
- 다만 더 간단하게 해결 가능한 코드가 명확히 있다면, 불필요하게 계층, 인터페이스, 패턴, 추상 클래스를 늘려 코드를 부풀리지 않는다.
- 추상화는 중복을 줄이거나, 변경 가능성을 격리하거나, 테스트 가능성을 높이거나, 의존성 방향을 지키는 실질적 이유가 있을 때 도입한다.
- 한 번만 쓰이는 단순 로직, 명확한 조건문, 작은 private 메서드로 충분한 문제는 과도한 패턴보다 간단한 구현을 우선한다.
- 간단한 구현을 선택하더라도 도메인 규칙의 위치, 의존성 방향, 테스트 가능성, 명명, 예외 처리의 명확성은 유지해야 한다.

## Code Change Workflow

- 코드 수정 전에는 반드시 `main` 브랜치와 `dev` 브랜치 이외의 브랜치에서 작업해야 한다.
- 현재 작업 브랜치가 지정되어 있지 않거나 `main` 또는 `dev`라면, 코드 수정 전에 사용자에게 이동할 브랜치를 제안해야 한다.
- 코드 수정 작업은 전체적으로 TDD(테스트 주도 개발)의 흐름을 따라야 한다.
- 이 흐름을 통해 안정적인 개발과 테스트 가능한 시스템을 구성해야 한다.
- 커밋 시 `docs/commit.md`의 커밋 컨벤션을 따라야 한다. 현재 저장소의 파일명이 대문자로 되어 있다면 `docs/COMMIT.md`를 기준으로 한다.
- 코드 수정 전에는 사용자의 명령을 바탕으로 이번 작업의 성공 지표를 먼저 정의한다.
- 성공 지표는 해결해야 할 기능, 수정해야 할 버그, 통과해야 할 테스트, 지켜야 할 제약 조건처럼 확인 가능한 형태여야 한다.
- 작업 중에는 성공 지표를 만족하는지 반복적으로 확인하고, 만족하지 못했다면 테스트 작성, 구현, 리팩토링, 검증을 다시 수행한다.
- 작업 완료 시에는 어떤 성공 지표를 만족했는지, 어떤 테스트 또는 검증을 수행했는지 사용자에게 보고한다.
